#include <iostream>
#include "Dictionary.h"
#include "Grid.h"

void findMatches(Dictionary dict, Grid grid);

int main(){
  return 0;
}

void findMatches(Dictionary dict, Grid grid)
{
  (void) dict;
  (void) grid;
}
